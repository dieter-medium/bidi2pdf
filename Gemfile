# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in bidi2pdf.gemspec
gemspec

# override chromedriver-binary gem to use the latest version from GitHub
# or use local version with
# bundle config local.chromedriver-binary <YOUR PATH TO>/chromedriver-binary
# gem "chromedriver-binary", github: "dieter-medium/chromedriver-binary", branch: "main"
